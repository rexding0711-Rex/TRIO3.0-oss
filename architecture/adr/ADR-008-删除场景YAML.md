# ADR-008: 删除场景YAML——执行器为过度设计
- 日期: 2026-07-09
- 状态: accepted
- 背景: due_diligence.yaml 存在但57 runs中零次被消费。v2方案建议150行Bash执行器
- 决策: 删除YAML。场景执行由CLAUDE.md触发词+现有协议组合覆盖。独立执行器为过度设计
- 后果: 减少死代码。如未来需要声明式场景编排→从协议层重构而非单独执行器
