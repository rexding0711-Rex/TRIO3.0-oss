# dsh 吸收总结报告（2026-08-14）

> 对象：DeepSeek AI 官方 agent harness（`dsh`，7404 文件 monorepo）
> 周期：单日三轮深挖 + 落地 + 验证 + 收敛 + 遗留处理
> 定位：这是把外部系统能力吸收进 TRIO 3.0 的完整过程记录，供复盘与复用。

## 一、过程全景

```
① 全量阅读(1轮)   → README/AGENTS.md/notes库/skills/defensive → 首轮 P0 落地
② 差距扫描(2轮)   → 4并行agent(skills/packages/docs/治理) → 差距矩阵 → P0 四项落地
③ 实现层深挖(3轮) → 3并行agent(compaction/persistence/notes全覆盖) → 实现层落地
④ 收敛           → 门禁实测验证 + 学习草稿审阅(4 applied/1 rejected) + 路线图收敛
⑤ 遗留处理       → memory-weights格式修复 + state-catalog接入full-system-verify
```

## 二、落地清单（三轮回合计 8 项）

| # | 落地项 | 文件 | 来源机制 |
|---|--------|------|---------|
| 1 | 门禁 A 升级（变更叙事 WARN + 读者视角测试 + verify-world） | delivery-gate.sh + CLAUDE.md | dsh-trim-cot-leakage / testing |
| 2 | Post-Mortem 复盘模板 | config/protocols/postmortem-template.md | dsh postmortem |
| 3 | 防御性模式（5 规则） | config/protocols/defensive-patterns-trio.md | dsh defensive-patterns + notes |
| 4 | 报告精简纪律（命题守恒） | config/protocols/report-editing-discipline.md | dsh-prose-standard |
| 5 | decision-log 归档 | scripts/decision-log-archive.py | dsh 归档冻结 |
| 6 | 持久化纪律 | config/protocols/persistence-discipline.md | dsh session-persistence |
| 7 | 生成式 state 目录 | scripts/state-catalog.py + docs/state-catalog.md | dsh gen-persistence-catalog |
| 8 | 学习草稿 4 条入库 | knowledge/methods/learning/（共 5 条） | magi LearningDraft |

## 三、Bug 修复（3 个）

1. learning-draft.sh 同秒 ID 冲突（秒时间戳 + PID 后缀）
2. state-catalog.py frozenset 遍历依赖 hash seed（排序遍历）
3. state-catalog.py 行数敏感误报（改 schema 特征比较）

## 四、验证结论

- **门禁 A 实测**：3 份真实报告，正常 0 误报；WARN 级正确（FAIL 级会让讨论门禁的文档自阻断成悖论）
- **full-system-verify**：27 → 28 项全通过（接入 state-catalog --check）
- **state-catalog 价值实证**：首次运行即抓到 2 个真实数据问题（memory-weights 非 JSONL、decision-log 字段演进）

## 五、关键决策

1. **全部吸收 ≠ 全部落地**——只落地轻量可逆的 P0；架构级项记路线图（防模板通胀）
2. **吸收"治理机器"而非"代码架构"**——dsh 的 Cordis 插件外壳不可搬，机制思想（事件溯源/投影/安全注入）留作路线图
3. **TRIO 认识论层比 dsh 强**（counter_evidence/minority/prediction_contract/calibration）——吸收方向是补治理机器，不照抄笔记格式
4. **收敛为待触发清单**——6 项机制遇真实需求才落地

## 六、待触发清单

tool-result-pruner · naming-contract · repeat-tool-reminder · feedback 双契约 · persistence envelope 化 · supersession 审计

## 七、复盘（可复用方法论）

1. **外部系统吸收五步法**：全量阅读 → 差距扫描（并行 agent）→ 实现层深挖 → 收敛验证 → 遗留处理
2. **验代码不轻信文档**：README 声称 vs 实际（magi 前例教训复用）
3. **生成式工具必须跨进程字节稳定**（frozenset/hash seed 教训）
4. **吸收防囤积**：落地前核实现状（reject 已有）、误杀词按域裁剪、路线图待触发
