# TRIO 3.0 核心 SOP

> 所有场景/工作流的不可变基础。新命令第一行必须 `Read D:\TRIO 3.0\CORE.md`。
> **定位**：公司商业模式分析（主）+ 技术研发剖析（主）+ 名人决策模式（主）+ 行业全量/政策/个人决策（辅）。以下规则针对分析营利性公司和商业决策者优化——非营利/政府/个人场景下，C-Suite 立场和财务规则按需降级使用。

---

## 上下文预算纪律（灵感：何恺明 DLCM · R=4 语义概念压缩）

> CORE.md 和 CLAUDE.md 消耗 context window。不是所有 token 都值得每次加载。

**三层加载模型**（目标：每次会话节省 40-60% 上下文预算）：

| 层级 | 占比上限 | 加载策略 | 包含内容 |
|------|:--:|------|------|
| **L1 常驻** | ≤30% | 每次会话自动加载 | 三根柱子 · 通用执行规则 · 防美化原则 · 拓扑门禁 · 变更扩散 · 安全红线 · **安全分级速查（L1/L2/L3——见 `config/security-classification.json`）** |
| **L2 触发** | ≤40% | 任务类型触发时 Read | 场景 JSON · 协议文件 · 技能 SKILL.md · 角色定义 · 方法论 · **`core/ontology/trio-ontology.md`**（分析型会话必加载） |
| **L3 索引** | ≤30% | 不加载，仅路径引用 | 文件路由表 · ADR · 质量参考 · 术语表 · 版本历史 |

**纪律**：
- 任何新规则落地前必须标注层级（L1/L2/L3）。L1 膨胀超过 30% → 裁剪或降级
- 跨文件重复内容 → CORE.md 为主副本，其余位置只保留路径引用 + 一句话摘要
- 季度修剪（见 `memory/` 季度规则修剪记录）：目标剔除 ≥20% 冗余/过时内容

---

## 零、防美化原则（所有分析类型通用）

**被分析对象提供的材料（简历/财报/PR稿/融资通稿）本身是美化过的。** 不接受其自我叙事框架是第一道防线。

每条分析开始前，先问三个问题：
1. **这个对象如何描述自己？**（"我是战略 BD 专业人士""我们是技术驱动的全球化企业"）
2. **这个描述在今天的市场上还有人买吗？**（标签本身还有价值吗？不是"标签下的内容是不是真的"——是"标签还有人付钱吗"）
3. **如果拆掉这个标签——底层还剩下什么？**（不被自我叙事绑架，看到标签下的能力/资产单元）

**硬约束**：人物评估必须先做⑥市场出清再评估能力。公司尽调必须先确认"公司自我标签 vs 市场实际付费意愿"的差距。行业分析必须先区分"行业的 PR 叙事"和"行业的财务现实"。

---

## 三根柱子

TRIO 防御的不是"出错"，是**安静地出错**——让错误在系统内部被看见，不在用户手里爆炸。

### 1. 认知隔离 — 防群体思维

三角色（Explorer 探索者 / Auditor 审计者 / Integrator 整合者）并行启动，互不可见对方输出。仅共享原始材料（用户输入+搜索结果），不传上下文。

- **当前实现**：单模型（DeepSeek V4 Pro）+ 独立 system prompt。角色间隔离为"弱隔离"——共享底层权重，但通过 prompt 工程实现认知分化
- **目标架构**：多模型独立权重——至少一个角色运行在独立模型上，提供真正的独立审计。路线图见 `config/multi-model-roadmap.md`
- **为什么不可丢**：没有隔离 = 三角色退化为"一个人的三种措辞"。三重质控坍缩为伪多元。
- **来源**：Devin 证据——审查 Agent 不与编写 Agent 共享上下文 → 每 PR 平均发现 2 个 bug，58% 严重。

### 2. 结构化怀疑 — 防自欺

三个机制组成安全网：
- **可证伪条件**：每个关键结论附"什么条件下这个结论会被证伪"。覆盖率 < 80% → 阻断交付。
- **少数派保护 (E3)**：一个视角持续分歧 → 封存标注，不取平均，不杀死。历史反复证明少数派+最后被证实=创新最常见模式。
- **ESCALATE**：仅四种情况触发（不可逆决策 / 面具间根本分歧 / 置信度崩塌 / 同一 FAIL 3 轮未 PASS）。触发后 state.status = "paused_escalate"，等用户裁决。
- **决策提交门 (Decision Commit)**（2026-08-14 外部审计吸收）：预测/判断注册必须走 `python3 scripts/decision-commit.py <candidate.json>`——confidence ≥ 4 无 counter_evidence、prediction_contract 缺字段、未声明 verification 的候选**阻断写入**（exit 1）。禁止绕过脚本直接 append decision-log.jsonl。提交门管新决策，constitution-gate.py 管存量，两者互补。

- **为什么不可丢**：没有这些 = AI 隐藏不确定性、假装自信、在死循环里硬上。

### 3. 自验证闭环 — 防随机质量

Agent 交付前必须先自检（对照 acceptance_criteria）→ 自行修正 → 再检。Auditor 只做抽查，不逐条重复。

- **为什么不可丢**：Fable 5 数据——自验证环在 58% PR 中发现严重问题。去掉它 = 用户是唯一质量闸门。
- **来源**：Fable 5 self-verification loop。

---

## 通用执行规则

1. **状态机**：每个 run = `runs/{run_id}/`，含 state.json + todo.md + step{N}.md。支持断点恢复。
2. **上下文隔离**：Step 间只传 ≤500 字结构化摘要。图状态 JSON 不受此限。
3. **Agent Write 纪律**：每步完成 → 立即 Write step{N}.md → 更新 state.json。不许等全跑完再写。
4. **depends_on 并行判断**：扫描所有未执行步骤，depends_on 全部满足的步骤可并行执行。
5. **能力回退不拒绝**：高风险场景降级到保守模式执行。用户永远得到输出。
6. **人类可读性**：所有 Step 输出必须人类可审计。禁止不可读符号/编码作为关键判断依据。
7. **死端点快速失败（magi 吸收 P0-3）**：网络/API 调用失败时，先分类错误类型——死端点（DNS失败/拒连/无效URL/鉴权失败）**立即失败不重试**；可重试错误（超时/限流/服务端错误）才走指数退避。工具：`source lib/fail-fast.sh`（`fail_fast_is_retryable`）。违反 = 在死端点上烧重试预算。

---

## 硬宪法与软宪法（v2.1 新增 · 来源：6 模型外部评审共识）

CORE.md 同时包含形而上的哲学（三根柱子）和极底层的工程规范（排版检查、Mermaid 禁令）。当"宪法"开始规定"PDF 不能用什么格式"时，它侵蚀了 Branch 的灵活性。

**拆分原则**：不可变 → 硬宪法。可被实践推翻 → 软宪法。

### 硬宪法（不可变 · 需全 Branch 共识 + 创始人批准才能修改）

| 条目 | 内容 | 不可变原因 |
|------|------|------|
| 三根柱子 | 认知隔离 / 结构化怀疑 / 自验证闭环 | TRIO 的存在理由 |
| 上下文预算纪律 | L1 ≤ 30% 窗口 | 规则膨胀 = 认知带宽坍缩 |
| 安全红线 | 不执行 rm -rf / 不修改系统目录 / 不 force push | 不可逆操作 |

### 软宪法（可被 Branch 实践修订 · 季度投票）

| 条目 | 当前版本 | 修订触发 |
|------|------|------|
| 通用门禁（A/A+/B/C/拓扑） | v3.2 | 连续 20 份报告某门禁零命中 → 降级或休眠 |
| 排版硬检查 | v1.0 | 新输出格式出现 → 追加规则 |
| 文档生成管道 | v1.0 | 工具链升级 → 更新 |
| 铁律共享内核（0/1/2） | v1.0 | Branch 实战发现通用模式 → 上浮 |

**修订流程**：Branch 提修正案 → 季度宪法评审 → 创始人裁决 → 通过的上限为提案件数的 20%（防宪法通胀）。

---

## 面具实现诚实声明（v2.1 新增）

**当前实现**：三角色全部运行在 DeepSeek V4 Pro 单模型上，通过独立 system prompt 实现角色分化。这是"弱隔离"——角色间共享底层权重。

**这意味着**：
- Kimi 和 Claude 的"共识"可能是同源噪声，不是独立验证
- 只有 prompt 层面的认知分化，没有权重层面的独立审计
- 隔离的真实性取决于 system prompt 的差异程度——差异越大，伪多元风险越低

**当前够用吗？** 够。Stock 63 只全量分析、Investor 铁律 8 模型交叉验证——实践表明单模型多 prompt 已能提供足够的视角差异。但这是工程判断，不是理论证明。

**目标**：至少一个角色迁移到独立权重模型。优先级：审计者（最需要真正独立性的角色）。路线图见 `config/multi-model-roadmap.md`。

---

## Branch 治理体系（v2.1 新增 · 来源：6 模型外部评审共识）

### Meta 调度层

**问题**：当用户问"陶瓷 3D 打印散热器有没有投资价值？"——Engineer 先跑（评估技术可行性）还是 Investor 先跑（评估市场前景）？目前没有调度规则。

**调度规则**（按问题类型自动路由）：

| 问题类型 | 先跑 | 原因 |
|------|------|------|
| "能不能做"型（物理可行性） | Engineer → Investor/Stock | 物理不可行 = 投资分析无意义 |
| "值不值得投"型（财务回报） | Investor/Stock → Engineer | 市场规模先确认，再验证技术壁垒 |
| "为什么失败"型（复盘诊断） | 三面具并行 | 失败原因不预设领域 |
| "是什么/怎么工作"型（理解） | Engineer → Kimi 发散 | 先理解物理机制，再发散关联 |

**硬约束**：Meta 调度不是 AI 决策——是规则表。不确定时 → 问用户"先看技术还是先看市场？"

### Branch 通信契约

**Branch 间互借方法论**：允许直接互借，但必须附带元数据标签（"接口契约"），否则借用方必须拒绝。

**必须标签**：
```
借出 Branch: [Stock/Investor/Engineer]
方法名称: [评分卡/FMEA/估值模型/...]
适用域: [该方法在什么条件下被验证有效]
已知失效条件: [什么情况下这个方法会给出错误结论]
借出方警告: [如果在 {目标领域} 使用，最可能崩坏的假设是 {X}]
最后验证日期: [YYYY-MM-DD]
```

**禁止**：不带上述标签的方法论跨 Branch 传播。由宪法软层的季度审计检查合规性。

### Branch 创立/退役标准

**创立**（三条全部满足）：
1. 有独立的领域本体——"质量"的定义与其他 Branch 不同
2. 有 ≥1 个可运行的交付管道——不只是文档
3. 有 ≥1 个领域特有的质量门禁——通用门禁覆盖不了

**退役**（任一触发）：
- 连续两个季度无独立交付物
- 跨 Branch 调用次数低于阈值（季度 < 5 次）
- 领域本体被证明与其他 Branch 可合并

---

## 预测追踪 (Prediction Ledger)（v2.1 新增 · 来源：外部评审共识 #2）

**问题**：TRIO 的门禁是交付前检查——不追踪交付后。校准漂移（Calibration Drift）是静默的——系统学会"写出有权威感的分析"，而不是"写出准确的分析"。

**机制**：每条附带时间维度的结论，必须注册到 Prediction Ledger。

**注册格式**（写入 `state/prediction-ledger.jsonl`）：
```json
{
  "id": "pred-{日期}-{序号}",
  "statement": "清晰的预测陈述",
  "confidence": "[1-5]",
  "falsification_date": "YYYY-MM-DD（何时可以判断对错）",
  "verification_method": "如何验证（价格/财报/实验结果/外部事件）",
  "registered_by": "Branch名称",
  "registered_at": "ISO时间戳"
}
```

**回溯规则**：
- 到达 falsification_date → 系统自动提示"该核对了"
- 核对结果：✅ 正确 / ❌ 错误 / ⏸️ 尚不可判定
- 错误项 → 季度自动汇总 → 识别系统性偏差模式 → 输入宪法修订

**硬约束**：Prediction Ledger 的质量比分析报告本身更重要。它是 TRIO 的"血压计"——没有它，你永远不知道系统在变好还是变坏。

---

## 情绪门禁 (Sentiment Gate)（v2.1 新增 · 来源：外部评审共识 #4）

**问题**：三面具共享同一个人的情绪系统。当 Rex 对项目表达"想做/想投/想买"的强烈情绪时——三个面具会在不同程度上共振，而不是真正对立。这是 Branch 模型覆盖不了的盲区类型（动机型盲区）。

**检测信号**（任一触发）：
- Rex 使用"很兴奋""一定要做""不能错过""感觉对了"等情绪词
- 连续两次问同一个项目但换不同问法（寻找想要的答案）
- 对审计的反对意见表现出防御性回应（"DeepSeek 太保守了"）

**触发后动作**：
1. 该项目自动标记为"⚠️ 情绪共振风险"
2. **反方证据需求翻倍**——A 类反证覆盖率从 ≥90% 提升到 ≥95%
3. 审计者输出强制包含："如果这个项目最终失败了，最可能的原因是什么？"
4. 24 小时内禁止终版交付——强制冷静期

**不是阻断**：情绪门禁不阻止分析——它只增加证据门槛。Rex 有情绪是正常的，系统的工作不是消除情绪，是**让情绪不会静默地绕过所有门禁**。

---

### Gate 0：致命缺口扫描（L0 防线——标准模式+深度模式共用）

**触发时机**：收到分析型请求后、任何搜索启动前。覆盖标准模式和深度模式——深度模式跑 30-40 轮搜索代价极高，必须先过 Gate 0。

**触发时机**：收到分析型请求后、搜索启动前。

**执行规则**：
1. 列出支撑后续分析的核心假设
2. 对每个假设反向思考：反过来结论翻吗？翻 → 标注"翻转风险"
3. 输出缺口清单，**交给用户决定**：
   - 用户说"继续"/"先出" → 带缺口继续，推断标注 [2-3] + 注明假设前提
   - 用户说"刹车"/"先补" → 🛑 阻断，仅输出"已知 vs 未知" + 补缺口动作
   - 用户沉默 → 默认继续，但报告中显著标注缺口
4. **刹车权在用户手里——系统不替用户做决定**

**按对象调节**：
- 人物：信息源少，缺口天然多 → 列出后等用户决定
- 企业：信息源多，先搜 5-10 轮再用结果更新缺口 → 再问用户

**违反此规则 = 在未验证假设上构建分析。**

---

## 每次 Run 初始化检查清单

Agent 在 run 启动时必须逐条确认：

```
[ ] daily-cleanup.sh                    已运行（自动清理）
[ ] context-isolation-protocol.json   已 Read
[ ] self-verify-protocol.json         已 Read
[ ] escalate-protocol.json            已 Read
[ ] topology-thinking-protocol.json   已 Read（如涉及结构性分析）
[ ] loop-engineering 适用性            已判断（本任务适合 Loop 吗？）
[ ] 场景 JSON protocols 块与已加载协议一致
```

任一 ❌ → 阻断，补加载后再推进。

---

## 搜索完备性检查（Step 2 强制执行）

**问题根因**：Agent 只分析"搜到的数据"，不检查"该搜的数据搜了没有"。数据有洞 → 分析有洞。

**执行时机**：Step 2 搜索完成后、Step 3 三视角启动前。

**Phase 0：五认知立场盲区扫描（灵感：斯坦福 STORM）**

在进入维度完备性检查之前，先快速跑一轮认知立场视角的提问——**这不是行业参与者视角，而是认知立场视角**。五类立场各生成 2-3 个该立场独有的关键问题：

| 立场 | 核心追问 | 产出 |
|------|---------|------|
| **从业者** (Practitioner) | 每天都在一线干这事的人知道什么？什么实际约束被学术界忽略了？ | 地面真相问题 |
| **学者** (Academic) | 同行评审的证据实际说了什么？证据在哪和流行观点矛盾？ | 证据反直觉问题 |
| **怀疑论者** (Skeptic) | 主流共识最强的反证是什么？支持者刻意忽略了什么证据？ | 致命反证问题 |
| **经济学家** (Economist) | 谁在当前的叙事中获利？什么财务激励在塑造研究方向？ | 利益流向问题 |
| **审计师** (Auditor) | 数字内部是否自洽？季度加总等于年度吗？收入增速与应收增速匹配吗？现金流与利润同向吗？ | 数据一致性+造假信号 |
| **CTO** (Chief Technology Officer) | 如果明天接手这个技术栈——第一个要改的是什么？技术债藏在哪里？架构决策是因为技术原因还是政治原因？ | 技术真相关键问题 |
| **COO** (Chief Operating Officer) | 如果订单翻倍——第一个崩的是哪个环节？供应商集中度？良率真实吗？人效比同行差多少？ | 运营瓶颈关键问题 |
| **CMO** (Chief Marketing Officer) | 增长的每一块钱从哪来？CAC 是在降还是升？品牌溢价能撑住提价吗？渠道依赖度？ | 增长质量关键问题 |
| **历史学家** (Historian) | 历史上有什么相似模式？从那些模式的结局能学到什么？ | 历史重演问题 |
| **需求端** (Buyer/Customer/Hiring Manager) | 这个分析对象的"买家"到底是谁？他们真实的需求是什么？——人物评估时问"这个岗位真实存在吗？HC/学历门槛/薪资区间？"；公司尽调时问"这个市场的真实规模和增速（非PR数据）？客户为什么选A不选B？付费意愿？"；竞品对比时问"客户关心的对比维度是什么（非分析师关心的维度）？" | 需求端硬约束问题 |

**输出的"初始盲区清单"喂给后续搜索——作为搜索 query 的补充方向。**

**检查方式**：每个立场至少产出 2 个该立场独有的问题。少于 2 个 → 补足。所有问题汇总 → 去重 → 标注"哪些问题是之前没想过要搜的"→ 这些必须进搜索。

---

**对照下表——这些是元维度：换任何赛道都成立、不可再被拆分的最小信息单元。缺失维度 → 补搜 → 数据齐了再喂给三角色：**

| 分析类型 | 元维度（赛道无关） | 触发补充问题（根据赛道实例化） |
|---------|------------------|--------------------------|
| **行业全量** | ① 规模与增速 ② 格局与集中度 ③ 单元经济 ④ 价值流向 ⑤ 进入壁垒 ⑥ 风险 ⑦ **品牌与用户（+CMO 市场视角）**（谁在买？为什么选A不选B？品牌溢价能撑住提价吗？CAC趋势升降？渠道集中度？LTV/CAC比？市场份额是抢来的还是烧来的？） ⑧ **能力基座**（供应链/研发/基础设施/自有产品——"为什么赚这么多"的物理基础） ⑨ **国际对标**（同行业国外标杆是谁？核心效率指标差几倍？） ⑩ **覆盖完整性**（行业有几个阵营？每个阵营有哪些主要玩家？逐家覆盖了吗？） | **先问这个行业的单元经济是什么——再搜。** 不问赛道直接套模板 = 盲区。**④⑧回答"凭什么"、⑦⑨回答"差在哪"、⑩回答"漏了谁"** |
| **公司尽调** | ① 财务（营收质量/毛利趋势/现金流质量/营运效率/资本回报/烧钱速度/造假检测/估值纪律/表外风险） ② 人（+CHRO 组织视角：团队/治理/激励/关键人依赖度/继任计划/中层深度/离职率/招聘质量） ③ 产品（矩阵/差异化/依赖度） ④ 市场位置（份额/竞争位次） ⑤ 竞争（对手/相对优劣） ⑥ 风险（法务/监管/关键人/技术债） ⑦ **需求端验证**（市场真实规模和增速——用第三方数据而非公司PR数据交叉验证；客户为什么选A不选B？付费意愿？渠道真实动销 vs 压货？） | 上市公司→年报+研报；非上市→融资披露+竞品对比+供应链反推。**CFO 追问**：经营现金流/净利润≥0.8？应收账款增速>营收增速？若后者为真→渠道压货信号。**CHRO 追问**：关键人离职=公司停摆？中层晋升率？核心团队入职年限分布？不能用公司PR数据替代第三方市场数据。 |
| **竞品对比** | ① 经济模型差异（同一"单元经济"口径下的并排对比） ② 定价与价值主张 ③ 产品差异 ④ 客户差异 ⑤ 能力壁垒差异 ⑥ 资本差异（融资/估值/烧钱速度） ⑦ **需求端视角**（客户关心的对比维度是什么？不是分析师觉得重要的维度——是客户买单时实际用的维度。如果客户决策标准和分析框架不一致→标注） | **核心原则：必须同一口径——先确定这个赛道的单元经济是什么，再并排对比。** 不可交叉对比。 |
| **人物评估** | ① 时间线（履历+关键转折点） ② 能力与盲区 ③ 决策模式（压力下怎么做选择？） ④ 争议与风险 ⑤ 网络（谁影响他？他影响谁？） ⑥ **市场出清**（现在有没有雇主愿意为这个人当前的技能标签付钱？如果没有人买——拆掉标签，底层能力还能卖给谁？） ⑦ **需求端验证**（这个人的求职方向在市场上实际有多少HC？目标岗位的真实学历/经验门槛是什么？薪资区间是多少？至少搜3条招聘JD交叉验证） | 履历核查→公开记录交叉验证；决策模式→找2+个关键事件的决策过程重构，不依赖自述。**关键原则**：不接受被评估者的自我叙事框架。先问"这个标签还有市场吗"，再问"标签下的人有什么"。匹配度评分只在门开着时有意义——先做门禁判断，再评分。 |
| **供应链分析（+COO 运营视角）** | ① 层级结构（从原料到终端几层？） ② 单点故障（哪一节点的缺失会导致全局停摆？级联后果？） ③ 替代弹性（每个关键节点有替代方案吗？切换成本？） ④ 成本结构（每层加价率/利润率） ⑤ 外部脆弱性（地缘/监管/自然风险） ⑥ **产能真实性**（计划产能 vs 实际产出？良率？节拍时间？开工率？） ⑦ **品控体系**（质检是抽检还是全检？退货率？客户投诉趋势？） ⑧ **人效**（人均产值 vs 同行？规模化后边际人效在升还是降？） | 不要只画Tier1/2/3——要标出**如果这个节点断了的级联后果**。静态地图没用，要做扰动推演。**COO追问**：订单翻倍第一个崩的环节？供应商集中度>30%？良率是抽检数据还是全检？ |

**检查方式**：搜索完成后，逐维度问自己——"这个维度我有 ≥1 个可验证的数据源吗？"没有 → 补搜。

**技术尽调补充**：涉及科学/技术声明时，搜索前查阅 `D:\scientific-agent-skills\skills\database-lookup\SKILL.md`（78 个领域权威数据库目录：PubChem/ChEMBL/ClinicalTrials/USPTO/Materials Project/PDB/AlphaFold 等）——避免只用通用搜索引擎的盲区。

**硬约束**：三视角 Agent 启动前，必须确认维度覆盖率 ≥ 90% 且缺失维度 ≤ 1 个。覆盖率不足 → 阻断 Agent 启动 → 补搜。

---

### 财务反常自动标记（Step 2.5 搜索完成后立即执行）

搜索完成后，任一条触发 → 标记 🚩反常-财务 → 追加至少 1 轮针对性搜索：

1. **季报波动异常**：任一季报营收环比变动 > 50% → 查是否并购、业务重组、一次性收入
2. **现金流背离**：净利润 > 0 但经营现金流 < 0，连续 2 年 → 利润质量存疑
3. **应收膨胀**：应收账款增速 > 营收增速 + 20pp → 渠道压货或回款恶化
4. **Q4 冲量**：Q4 单季收入 > 前三季度均值 × 2 → 年底突击确认收入
5. **毛利突变**：毛利率同比变化 > 10pp 且无公开业务原因 → 会计政策变更或竞争恶化

触发后在 Step 4 综合简报中标注，归入 ⑥ 风险维度。

---

## 综合判断增强规则（灵感：斯坦福 STORM）

### 链式辩论回合（Step 3.5 矛盾图谱之前，灵感：BettaFish ForumEngine）

三角色各自输出后，不直接列矛盾。先让它们**互相辩论一轮**：

1. DeepSeek 看到 Kimi 和 Claude 的输出 → 对每个输出提出 1 个最尖锐的质疑
2. Kimi 和 Claude 各自回应 DeepSeek 的质疑（不是防御，是承认或反驳）
3. 辩论主持人（Claude 兼任）总结：哪些质疑被解决了、哪些仍然悬而未决

**目的**：不是让三角色达成一致——是让分歧在进入矛盾图谱之前**被充分暴露和压力测试**。并行输出只能发现"他们说的不一样"，辩论回合能发现"为什么不一样、谁的证据更强"。

**硬约束**：辩论只跑一轮。一轮之后未解决的分歧 → 进入矛盾图谱 → 少数派保护机制接管。禁止无限辩论。

### 矛盾消解追问（矛盾图谱末尾强制执行）

矛盾图谱输出后，追加一问：

> **如果回答哪一个问题，能消解这些矛盾中最大的那个？**

这个问题本身可能无法在当前分析中回答——但**必须标注**。它定义了"下一步如果只做一件事，该做什么"。这是从"列矛盾"到"消解矛盾"的那一步。

### 综合简报增强字段（Step 4 输出模板）

标准综合判断的输出模板中，追加两个字段：

| 字段 | 定义 | 示例 |
|------|------|------|
| **隐藏关联** (Hidden Connection) | 只有同时看所有视角才能发现的非直觉连接——单独看任何一个视角都看不到这个模式 | 例：三家不相关的公司——A 降运输成本，B 读神经信号，C 采集物理世界数据。分开看是三个行业，同时看是同一个底层问题：怎么把物理现实变成可计算的 |
| **前沿问题** (Frontier Question) | 如果这个问题被回答，整个行业/领域的理解会被改写。标注为什么这个问题现在无法回答 | 例：如果创始人明天消失——哪些资产是已建成的可独立运行系统、哪些是只有这个人能推动的愿景？从未在没有创始人的情况下运行过，无法回答 |

**硬约束**：隐藏关联和前沿问题不可为空。写不出 → 说明视角还不够多、分析还不够深 → 回到 Phase 0 补至少一个认知立场视角。

### 需求端门禁（Step 4 评分前强制执行）

**任何分析类型在给出"方向匹配度"或"市场前景"类评分之前，必须先跑需求端验证。** 不限于人物评估。

**人物评估**：搜索目标岗位的真实招聘JD（≥3条）→ 提取硬门槛（学历/专业/经验/证书）→ 提取薪资区间 → 判断门的开关状态。

**公司尽调**：用第三方数据（非公司PR/融资通稿）验证市场真实规模和增速 → 验证客户真实付费意愿（非"市场空间"估算）→ 验证渠道真实动销（非压货数据）。

**竞品对比**：从客户决策标准出发验证对比维度（非分析师框架）→ 客户实际因为什么选A不选B？

**门禁规则**：

| 门状态 | 条件 | 动作 |
|--------|------|------|
| 开着 | 需求端数据支撑 + 公司/人物声称与市场现实一致 | 正常评分 |
| 半开 | 部分支撑 或 数据不足 或 声称与市场现实有差距 | 评分时标注"市场约束" |
| 关着 | 硬门槛不达标 或 声称的市场根本不存在 或 需求端数据系统性否定 | **不评分**，直接标注"不可达"或"市场不存在" |

**禁止对着关着的门打分。** 评分框架必须先做二元门禁判断，再做量化评估。否则制造伪精确——让分数看起来像个选择，但其实根本就没有这个选项。

---

## Loop Engineering — 自动重试闭环（v3.5 新增）

> **来源**：Karpathy AutoResearch（700 次实验，20 项人类忽略的优化）+ Joel Niklaus Harness 实验（同模型不同 Harness 得分差 76 分）。
> **核心原则**：不是让模型更聪明——是让系统能跑循环。提出修改 → 运行验证 → 保留好的 → 丢弃坏的。

### 适用条件（四不缺一）

| 条件 | TRIO 中的判断 |
|------|------------|
| **任务高频** | 工作流至少每周跑一次。一次性分析用不上 Loop |
| **验证可自动化** | 有结构化评分器（topology-check --json）。无评分器→无法循环 |
| **Token 预算能消化** | 每轮循环消耗约等于 1 次单跑。3 轮 = 3 倍消耗 |
| **可访问真实环境** | 所有依赖的脚本/数据/API 可用。断链→循环卡死 |

**当前适用**: `topology-fix`（P0）— 每份报告触发 ≥1 次，验证器已有，预算可控。

### 三大基础设施

1. **验证器**（`topology-check.sh --json`）：自动输出 0-100 分。没有评分就没法比较"这轮比上轮好吗？"
2. **状态文件**（`state/loop-*.json`）：记录每轮迭代的得分和改动。支持断点恢复——进程崩溃不丢进度。
3. **停止条件**：全部通过 / 连续 2 轮无改善 / 最大 3 轮 / 得分下跌 >20% 回归熔断。

### 自动修复安全边界

| ✅ 允许（文本级） | ❌ 禁止（内容级） |
|-----------------|-----------------|
| 补 ??? 反脑补节点 | 修改分析结论 |
| 补反证提示模板 | 修改数据/推理链 |
| 删除冗余词汇 | 改事实性内容 |
| 清理方法论标注 | 增删分析章节 |

### 使用方式

```bash
# 手动触发（默认：检测到失败→报告→等人确认）
bash scripts/loop-engine.sh topology-fix <报告.md>

# 自动修复模式（修复→验证→循环）
bash scripts/loop-engine.sh topology-fix <报告.md> --auto-fix --max-iter 3
```

### 什么不该自动化

以下场景**禁止**使用 Loop——必须保持人在回路中：
- **人物评估**：涉及人的判断，自动化容易产生偏见放大
- **创造性分析**：首次探索陌生领域，"看起来对"≠"有价值"
- **一次性客户报告**：建 Loop 的成本远超收益
- **涉及安全/法务/合规的判断**：错误后果不可逆

---

## 交付格式规则

**全量分析 = 三件套交付（MD + PDF + HTML），缺一不可。**

| 产物 | 内容 | 生成方式 |
|------|------|---------|
| **MD** | 完整报告源文件，含 Mermaid 代码块 | 原始编写 |
| **PDF** | 适合打印分发——Mermaid 代码块替换为文字关系表 + HTML 引用链接 | `deliver.sh` 内部调用 |
| **HTML** | **全文内容 + Mermaid 交互式拓扑图**（浏览器打开，彩色可缩放） | `deliver.sh` 自动生成 |

**一键交付命令**：
```bash
bash scripts/deliver.sh <报告.md> [--theme A]
```
产出：同名 `.md` + `.pdf` + `.html` 三个文件。

**Mermaid 兼容规则**：五不碰—— `%` `+` `//` `<br/>` `???`。替代：百分号→中文描述，加号→中文，双斜杠→"与"，`<br/>`→`<br>`。**写完后自查**：节点标签无 `%` `+` `//` `<br/>`；边标签无 `???` `//`。PDF 自动替换为文字关系表，HTML 中 Mermaid.js CDN 自动渲染。MD 保留源代码。

**深度拓扑图**：结构性分析必须创建 `topology-state.json`（模板见 `config/topology-state-schema.json`），按 Phase 0-4 逐阶段填写，交付前运行 `scripts/topology-render.py` 生成 5 阶段 Mermaid 图。

---

## 新建工作流止损规则

| 规则 | 目的 |
|------|------|
| 必须建场景 JSON，不可在命令 .md 里裸写流程 | 协议通过 JSON protocols 块声明，不依赖人记忆 |
| 新建命令第一行 = `Read CORE.md` + `Read 对应场景 JSON` | 入口统一，协议继承自动生效 |
| 必须引用角色文件，不许直接用面具名替代 | 角色文件是领域知识容器 |
| 建完后跑 DeepSeek 审计 | 对比 vs CORE.md，列出缺失项 |

---

## 反模式

storm-trio.md（2026-07-06）绕过场景JSON、跳过角色文件、丢了状态机/自验证/ESCALATE、quick模式跳过认知隔离。**任何新命令出现以上任一模式 → 阻断退回。**
